on:
  repository_dispatch:
    types: [ copy-workflow ]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.client_payload.github.pull_request.title }}
      - run: echo ${{ github.event.client_payload.targets }}
      - run: echo ${{ github.event.client_payload.workflows }}
