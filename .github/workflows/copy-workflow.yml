on:
  repository_dispatch:
    types: [ copy-workflow ]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - run: jq . ${{ github.event.client_payload.github }}
      - run: jq . ${{ github.event.client_payload.targets }}
      - run: jq . ${{ github.event.client_payload.workflows }}
